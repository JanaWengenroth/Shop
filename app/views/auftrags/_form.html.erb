<%= form_for(@auftrag) do |f| %>
  <% if @auftrag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auftrag.errors.count, "error") %> prohibited this auftrag from being saved:</h2>

      <ul>
      <% @auftrag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datum %><br>
    <%= f.date_select :datum %>
  </div>
  <div class="field">
    <%= f.label :kunden_id %><br>
    <%= f.number_field :kunden_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
